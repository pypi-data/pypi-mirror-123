[tool.poetry]
name = "gan-mealnizer"
version = "2.0"
description = ""
authors = ["Gan Mahmud <ganmahmud@users.noreply.github.com>"]
readme = "README.md"

[tool.poetry.scripts]
gan-mealnizer = "gan_mealnizer.main:app"

[tool.poetry.dependencies]
python = "^3.9"
typer = {extras = ["all"], version = "^0.4.0"}

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
