from setuptools import setup

setup(name='mm_udawsml_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['mm_udawsml_distributions'],
      author='margit',
      zip_safe=False)
