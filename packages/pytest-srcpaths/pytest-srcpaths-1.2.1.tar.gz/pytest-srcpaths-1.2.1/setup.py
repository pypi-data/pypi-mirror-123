#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

install_requires = \
['pytest>=6.2.0']

extras_require = \
{'test': ['tox']}

entry_points = \
{'pytest11': ['plugin = pytest_srcpaths']}

setup(name='pytest-srcpaths',
      version='1.2.1',
      description='Add paths to sys.path',
      author=None,
      author_email='Brian Okken <brian+pypi@pythontest.com>',
      url=None,
      py_modules=['pytest_srcpaths'],
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.7',
     )
