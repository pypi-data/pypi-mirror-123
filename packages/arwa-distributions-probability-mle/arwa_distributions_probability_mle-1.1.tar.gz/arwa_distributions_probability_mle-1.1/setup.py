from setuptools import setup

setup(name='arwa_distributions_probability_mle',
      version='1.1',
      description='Gaussian distributions',
      packages=['arwa_distributions_probability_mle'],
      zip_safe=False)
