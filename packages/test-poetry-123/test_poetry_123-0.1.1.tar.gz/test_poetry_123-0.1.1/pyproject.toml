[tool.poetry]
name = "test_poetry_123"
version = "0.1.1"
description = ""
authors = ["Robert Kowalski <rkowalski@algopolis.ai>"]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
