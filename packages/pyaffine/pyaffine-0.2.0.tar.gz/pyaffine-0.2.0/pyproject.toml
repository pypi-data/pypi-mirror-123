[tool.poetry]
name = "pyaffine"
version = "0.2.0"
description = "Thesaurus which uses the Datamuse API."
authors = ["davidshivaji <davidshivaji@gmail.com>"]
readme = "README.md"
license = "MIT"

[tool.poetry.scripts]
affine = "pyaffine.main:start"

# [tool.poetry.scripts]
# poetry = 'poetry.console:run'

[tool.poetry.dependencies]
python = "^3.9"
python-datamuse = "1.3.0"
click = "^8.0.3"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
