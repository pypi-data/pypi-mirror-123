from setuptools import setup

setup(name='Bi_Gau_distributions',
      version='0.1',
      description='discrete variable probability distribution',
      packages=['Bi_Gau_distributions'],
      zip_safe=False)
