#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['hydra_plugins', 'hydra_plugins.experiment_sweeper_plugin']

package_data = \
{'': ['*']}

install_requires = \
['hydra-core', 'more-itertools']

extras_require = \
{'test': ['pytest']}

setup(name='hydra-experiment-sweeper',
      version='0.1.0',
      description='A hydra sweeper with configurable overrides for reproducible experiments',
      author=None,
      author_email='Marten Lienen <marten.lienen@gmail.com>',
      url=None,
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      python_requires='>= 3.8',
     )
