[tool.poetry]
name = "qikdb"
version = "4.0.1"
description = "Simple, and easy to use Python Database using JSON."
authors = ["Andrew Bordis"]

[tool.poetry.dependencies]
python = "^3.8"


[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
