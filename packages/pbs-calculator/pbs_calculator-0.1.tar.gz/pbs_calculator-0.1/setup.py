from setuptools import setup

setup(name='pbs_calculator',
      version='0.1',
      description='Simple calculator',
      packages=['pbs_calculator'],
      author='Prema S',
      zip_safe=False)
