from setuptools import setup

setup(name='distributions_shenderovv',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_shenderovv'],
      zip_safe=False)
