from setuptools import setup

setup(
      name='guass_binomial_probability',
      version='0.1',
      author= 'Jeremiah Binyiako',
      description='Gaussian and Binomial distributions',
      packages=['distributions'],
zip_safe=False)
