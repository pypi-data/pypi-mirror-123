from setuptools import setup

setup(name='m2im_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['m2im_distributions'],
      zip_safe=False)
